# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
path: /content/datasets/lower  # dataset root dir
train: images/train  # train images (relative to 'path') 128 images
val: images/valid  # val images (relative to 'path') 128 images
test:  # test images (optional)

# Classes
nc: 17  # number of classes
names: ['hip_truster', 'leg_extension', 'leg_press', 'lying_leg_curl', 'v_squat', 'hack_squat', 
          'thigh', 'glute_kick_back', 'chest_press', 'chest_fly', 'seated_dip', 'shoulder_press', 'seated_row', 
          'pec_deck_fly', 'hack_press', 'lat_pull_down', 'arm_curl']  # class names